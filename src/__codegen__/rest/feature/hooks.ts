'use client';

/**
 * Generated by orval v8.9.1 🍺
 * Do not edit manually.
 * Feature Service
 * Feature flag management for PACT
 * OpenAPI spec version: 1.0.0
 */
import useSwr from 'swr';
import useSWRMutation from 'swr/mutation';
import type { Key, SWRConfiguration } from 'swr';
import type { SWRMutationConfiguration } from 'swr/mutation';

import type { AxiosError, AxiosRequestConfig } from 'axios';

import type { Feature, UpdateFeatureRequest } from './types';

import {
  getFeaturesResponse200,
  getFeaturesResponseSuccess,
  getGetFeaturesUrl,
  getFeaturesResponse,
  getFeatures,
  getGetFeaturesKey,
  updateFeatureResponse200,
  updateFeatureResponse404,
  updateFeatureResponseSuccess,
  updateFeatureResponseError,
  getUpdateFeatureUrl,
  updateFeatureResponse,
  updateFeature,
  getUpdateFeatureMutationFetcher,
  getUpdateFeatureMutationKey,
} from './fetchers';

export type GetFeaturesQueryResult = NonNullable<
  Awaited<ReturnType<typeof getFeatures>>
>;

/**
 * @summary List all feature flags
 */
export const useGetFeatures = <TError = Promise<unknown>>(options?: {
  swr?: SWRConfiguration<Awaited<ReturnType<typeof getFeatures>>, TError> & {
    swrKey?: Key;
    enabled?: boolean;
  };
  fetch?: RequestInit;
}) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getGetFeaturesKey() : null));
  const swrFn = () => getFeatures(fetchOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions
  );

  return {
    swrKey,
    ...query,
  };
};

export type UpdateFeatureMutationResult = NonNullable<
  Awaited<ReturnType<typeof updateFeature>>
>;

/**
 * @summary Update a feature flag's enabled state
 */
export const useUpdateFeature = <TError = Promise<void>>(
  id: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof updateFeature>>,
      TError,
      Key,
      UpdateFeatureRequest,
      Awaited<ReturnType<typeof updateFeature>>
    > & { swrKey?: string };
    fetch?: RequestInit;
  }
) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getUpdateFeatureMutationKey(id);
  const swrFn = getUpdateFeatureMutationFetcher(id, fetchOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
