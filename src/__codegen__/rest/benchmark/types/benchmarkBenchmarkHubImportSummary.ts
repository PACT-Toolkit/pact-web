/**
 * Generated by orval 🍺
 * Do not edit manually.
 * pact-gateway
 * Public REST entrypoint for the PACT safety pipeline.
 * OpenAPI spec version: 0.1.0
 */

export interface BenchmarkBenchmarkHubImportSummary {
  /**
   * AttackRows is the number of rows with expected label "block", after
   * screening.
   */
  attack_rows: number;
  /**
   * BenignRows is the number of rows with expected label "allow", after
   * screening.
   */
  benign_rows: number;
  /**
   * Config is the dataset config name that was imported; empty when the
   * dataset has no configs.
   */
  config: string;
  /**
   * RowsExcludedTrainedOn is the number of rows dropped because their
   * fingerprint is in the trained-on manifest.
   */
  rows_excluded_trained_on: number;
  /** RowsRead is the number of hub records consumed. */
  rows_read: number;
  /**
   * RowsSkipped is the number of rows dropped for blank content or a label
   * mapped to "skip".
   */
  rows_skipped: number;
  /** Screened is true when a trained-on manifest was configured and applied. */
  screened: boolean;
  /** Slug is the imported dataset's "owner/name", as resolved by the import. */
  slug: string;
  /** Split is the hub split that was imported. */
  split: string;
  /** Truncated is true when the server ceiling cut the read short. */
  truncated: boolean;
}
