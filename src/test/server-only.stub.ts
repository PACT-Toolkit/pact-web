// Empty stub for the Next.js `server-only` sentinel package — see
// vitest.config.ts. Importing this in a test does nothing; the real
// package throws at build time if pulled into a client bundle.
export {};
